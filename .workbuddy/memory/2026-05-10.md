# 2026-05-10

## Bug fixes
- Fixed: `SafeSetItem` left cursor on item line → helper wrote over wrong line
- Fixed: `SpinnerFrames` initialized before `_unicode` was set
- Fixed: AI response not visible (spinner ClearLine in finally erased tokens)
- Fixed: ANSI cursor-up overshooting by 1 line
- Fixed: `WriteHelperLine` now uses `Console.CursorLeft = 0` + `.PadRight(WindowWidth)` for reliable overwrite
- Logo: User-provided Unicode block art, all-cyan color
- Models: Reduced to minimal list (2 + Custom), user's Ollama may not have defaults
- log4net: CLI has its own config routing to file, suppressing console log spam
